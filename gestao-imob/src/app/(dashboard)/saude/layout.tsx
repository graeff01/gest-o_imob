import { TechnicalOnlyLayout } from "@/components/layout/technical-only-layout";

export default async function SaudeLayout({ children }: { children: React.ReactNode }) {
  return TechnicalOnlyLayout({ children });
}
